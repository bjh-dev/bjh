{
  "name": "bjh",
  "version": "1.0.0",
  "description": "A website project built with Sanity and Gatsby using typescript",
  "repository": "git@github.com:bjh-dev/bjh.git",
  "author": "Bryan Hickey <bryanjhickey@users.noreply.github.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "build": "pnpm run build --r",
    "dev": "pnpm run dev --r",
    "format": "pnpm run format",
    "build-studio": "(cd studio && npm run build)",
    "build-web": "pnpm bootstrap && (cd studio && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN npm run graphql-deploy) && (cd web && npm run build)",
    "graphql-deploy": "pnpm run graphql-deploy",
    "lint": "pnpm run lint"
  }
}
